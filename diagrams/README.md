# Diagrams

Mermaid source for the diagrams used in the docs. Render with the Mermaid CLI
or any Mermaid-aware Markdown viewer (GitHub renders `.mmd` in fenced blocks).

- `architecture.mmd` — model: shared backbone + three heads
- `training_pipeline.mmd` — data-to-deployment flow
- `hardware_connection.mmd` — webcam + board + display wiring
