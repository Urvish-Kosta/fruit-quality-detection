# Examples

- `config.yaml` — a ready-to-edit training configuration.

Quick start (after preparing `data/` per ../docs/DATASET.md):

```bash
python ../scripts/train.py --backend tensorflow --config config.yaml
```
